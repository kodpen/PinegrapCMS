# Development tree - never published
/dev/

# Internal documents: only the contribution guides live in the repo; plans
# and the change log stay on the development machine
/docs/*
!/docs/component-development-guide.md
!/docs/LANG_USAGE.md

# Local working files
*.log
/pinegrap/data/temp/
/pinegrap/data/cache/

# Real configuration - the repo holds only the empty template and config(default).php
config.local.php
.env
